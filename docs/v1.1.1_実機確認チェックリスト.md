# Field Validation v1.1.1 hotfix 実機確認チェックリスト

作成日: 2026-09-14
対象コード: main 1b4f1a0 + 未コミット変更（source/js/desktop.js, source/js/config.js）
ローカルテスト: 104 PASS / 0 FAIL

各項目の「結果」欄に PASS / FAIL / 未確認 を記入する。FAIL の場合は「メモ」に再現手順と Console の内容を書く。

---

## 0. 事前準備

### 0-1. 確認用ビルド

実機に入れるには plugin ZIP が必要。**配布用 ZIP はまだ生成しない**方針のため、確認用に一時パッケージ（例: `kintone-field-validation-plugin_v1.1.1-rc.zip`、`plugin/` 配下には置かない）を作るかどうかを先に決める。
同じ秘密鍵（`.ppk`）で署名すればプラグイン ID は変わらず、v1.1.0 からの「更新」として上書きできるので既存設定はそのまま引き継がれる。

```
[ ] 0-1 確認用 ZIP の作成方針を決定（作成する場合は manifest version は 1.1.0 のまま）   結果: (      )
```

### 0-2. 検証用アプリのフォーム

| フィールド名 | フィールドコード | 種類 | 用途 |
|---|---|---|---|
| 備考 | `備考` | 文字列（複数行） | A-1〜A-4 |
| 担当者 | `担当者` | ユーザー選択 | A-5, A-6 |
| 顧客名 | `顧客名` | 文字列（1行） | B, C, F |
| 金額 | `金額` | 数値 | F（AND/OR） |
| 契約開始日 | `契約開始日` | 日付 | F（日付） |
| 契約区分 | `契約区分` | ドロップダウン（新規／更新／解約） | F（選択肢） |
| 契約書 | `契約書` | 添付ファイル | F（ファイル） |
| 削除予定 | `削除予定` | 文字列（1行） | C（設定後にフォームから削除する） |
| 明細 | `明細` | テーブル（内に `品名` 文字列1行） | D, E |
| ステータス | プロセス管理を有効化（未処理→申請中→承認済） | | 任意 |

### 0-3. 旧設定（D・E 用）の作り方

v1.1.1 の設定画面はテーブル内フィールドを候補に出さないため、旧設定は **v1.1.0 で作ってから更新する**。

```
[ ] 0-3-a v1.1.0（現行配布 ZIP）を検証アプリに適用し、以下を設定して保存
        ルール1「契約時チェック」 有効
          IF  : ステータス が 申請中 （AND）
          THEN: 契約書 ファイルあり    message「契約書を添付してください」
                契約開始日 ≧ 今日     message「開始日は今日以降」
        ルール2「旧設定」 無効
          IF  : 金額 ≧ 1000000 （OR）
                削除予定 ＝ ABC
          THEN: 備考 空欄ではない          message「備考必須」
                明細.品名 空欄ではない     message「品名必須」
                削除予定 ＝ ABC            message「削除済み」
        結果: (      )
[ ] 0-3-b v1.1.1 確認用 ZIP で「プラグインの更新」を実行（設定が消えていないこと）   結果: (      )
[ ] 0-3-c 設定画面を開かずに、フォームから「削除予定」フィールドを削除して保存      結果: (      )
```

ブラウザの開発者ツール Console を開いた状態で以下を実施する。

---

## A. empty / not_empty（THEN）

準備: ルール「A」 有効、IF なし、THEN 1件だけを都度差し替える。

```
[ ] A-1 備考: 空欄である   + 空欄     → 保存できる                        結果: (      )
[ ] A-2 備考: 空欄である   + 値あり   → 備考にエラー、保存できない        結果: (      )
[ ] A-3 備考: 空欄ではない + 空欄     → 備考にエラー、保存できない        結果: (      )
[ ] A-4 備考: 空欄ではない + 値あり   → 保存できる                        結果: (      )
[ ] A-5 担当者: 空欄ではない + 0件    → 担当者にエラー、保存できない      結果: (      )
[ ] A-6 担当者: 空欄ではない + 1件以上 → 保存できる                       結果: (      )
```

## B. empty / not_empty（IF）

準備: ルール「B」 有効、IF「備考 が 空欄ではない」、THEN「顧客名 ＝ NEVER」（必ず NG になる検証）。

```
[ ] B-1 備考 空欄   → THEN が実行されず保存できる                          結果: (      )
[ ] B-2 備考 値あり → THEN が実行され、顧客名にエラーで保存できない         結果: (      )
```

## C. 不在 THEN（設定後にフィールド削除）

準備: 0-3 の状態（「削除予定」はフォームから削除済み）。ルール2 を有効にし、IF は「金額 ≧ 1000000」のみ残す（削除済み側の IF 行は無効なので、この確認では触らない）。

```
[ ] C-1 正常 THEN のみ（ルール1・NG: 契約書なし）→ 従来どおり契約書にエラー、保存停止     結果: (      )
[ ] C-2 正常 THEN のみ（ルール1・OK: 契約書あり、開始日=今日）→ 保存できる               結果: (      )
[ ] C-3 削除済み THEN のみ（ルール2 の THEN を「削除予定 ＝ ABC」1件にして金額 2000000）
        → JavaScript エラーなし／保存できる／Console に warn（ルール名・削除予定）／
          event.error による「入力内容にエラーがあります」が出ない                       結果: (      )
[ ] C-4 削除済み THEN + 正常 THEN（NG）（ルール2 THEN: 削除予定 ＝ ABC, 備考 空欄ではない／備考空欄）
        → 削除済みはスキップ（warn 1件）、備考にエラーで保存停止                        結果: (      )
[ ] C-5 削除済み THEN + 正常 THEN（OK）（同上／備考に値あり）→ 保存できる、warn 1件        結果: (      )
[ ] C-6 IF が不成立のとき（金額 100）→ warn が出ない（ログ過多にならない）               結果: (      )
```

## D. 既存設定の保持（最重要）

準備: 0-3 の状態で設定画面を開く。

```
[ ] D-1 設定画面がエラーなく開き、ルール1・ルール2 とも表示される                       結果: (      )
[ ] D-2 ルール2 THEN「明細.品名」の対象フィールドが「明細.品名（現在使用できないフィールド）」表示   結果: (      )
[ ] D-3 同行の演算子が「空欄ではない（現在使用できない設定）」表示（eq に変わっていない）   結果: (      )
[ ] D-4 ルール2 IF/THEN の「削除予定」が「削除予定（現在使用できないフィールド）」表示、
        演算子 ＝、値 ABC が残っている                                                  結果: (      )
[ ] D-5 何も触らず「保存」→ エラーなく保存完了                                          結果: (      )
[ ] D-6 再度設定画面を開き、ルール2 THEN 明細.品名 が field=明細.品名 / operator=空欄ではない /
        message=品名必須 のまま                                                          結果: (      )
[ ] D-7 ルール2 IF「削除予定 ＝ ABC」、THEN「削除予定 ＝ ABC / 削除済み」が不変            結果: (      )
[ ] D-8 ルール1（名前・有効・IF 2件・THEN 2件・メッセージ・日付条件）が不変               結果: (      )
[ ] D-9 ルール2 の名前「旧設定」・無効チェック・OR が不変                                 結果: (      )
```

## E. サブテーブル候補

```
[ ] E-1 「＋ ルール追加」→ IF のフィールド候補に「明細」内のフィールド（品名）が無い           結果: (      )
[ ] E-2 同 THEN のフィールド候補にも無い                                                 結果: (      )
[ ] E-3 日付条件の基準フィールド候補にテーブル内の日付が無い（テーブルに日付を追加して確認）  結果: (      )
[ ] E-4 E-1〜E-3 のあと保存しても、既存のルール2「明細.品名」行は消えていない               結果: (      )
```

## F. 正常系（各 1 ケース）

```
[ ] F-1 AND: IF 金額 ≧ 1000 AND 契約区分 が 新規 → 両方満たすときだけ THEN が動く            結果: (      )
[ ] F-2 OR : IF 金額 ≧ 1000 OR 契約区分 が 新規 → 片方でも THEN が動く                       結果: (      )
[ ] F-3 複数 THEN: 契約書 ファイルあり + 備考 空欄ではない → NG の項目だけにエラーが付く      結果: (      )
[ ] F-4 日付: 契約開始日 ≧ 今日 → 昨日=エラー、今日=OK、明日=OK                             結果: (      )
[ ] F-5 選択肢: IF 契約区分 が 新規／更新（いずれか）→ 解約では THEN が動かない               結果: (      )
[ ] F-6 添付ファイル（v1.1.1 で対象外化）: v1.1.0 で作成済みの THEN「契約書 ファイルあり」があっても
        未添付／添付どちらでも保存でき、Console に添付ファイル条件スキップの warn が出る。
        新規ルールのフィールド候補に「契約書添付」が出ない                                  結果: (      )
[ ] F-7 添付ファイル IF: v1.1.0 で作成済みの IF「契約書 ファイルなし AND 金額 ≧ 100」+ THEN「備考 空欄ではない」
        → 金額 200・備考空欄でも保存でき（ルールごとスキップ）、Console に IF 条件スキップの warn が出る   結果: (      )
```

## G. 追加画面・編集画面の両方

```
[ ] G-1 レコード追加画面（app.record.create.submit）で A-3（not_empty + 空欄）がエラーになる   結果: (      )
[ ] G-2 レコード編集画面（app.record.edit.submit）で A-3 がエラーになる                       結果: (      )
[ ] G-3 追加画面で C-3（削除済み THEN のみ）が保存できる                                    結果: (      )
[ ] G-4 編集画面で C-3 が保存できる                                                          結果: (      )
```

## H. 全体

```
[ ] H-1 上記すべてを通して Console に赤字の JavaScript エラー（Uncaught）が出ていない        結果: (      )
[ ] H-2 エラー時の右下パネル（条件チェック結果）が従来どおり表示・閉じられる                  結果: (      )
```

---

## 結果サマリ

| 区分 | 件数 | PASS | FAIL | 未確認 |
|---|---|---|---|---|
| 0 事前準備 | 4 | | | |
| A THEN 空欄 | 6 | | | |
| B IF 空欄 | 2 | | | |
| C 不在 THEN | 6 | | | |
| D 既存設定保持 | 9 | | | |
| E サブテーブル候補 | 4 | | | |
| F 正常系 | 6 | | | |
| G 画面 | 4 | | | |
| H 全体 | 2 | | | |

メモ（FAIL 時の再現手順・Console 内容）:

-
