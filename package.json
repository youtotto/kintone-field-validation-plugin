{
  "name": "kintone-field-validation-plugin",
  "private": true,
  "description": "Test runner for the Field Validation plugin (no runtime dependencies)",
  "scripts": {
    "test": "node --test \"test/**/*.test.js\""
  },
  "devDependencies": {
    "jsdom": "29.1.1"
  }
}
